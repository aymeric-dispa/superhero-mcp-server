{
  "name": "superhero-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for connecting to the SuperHero API (Cloudflare Workers)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "keywords": ["mcp", "superhero", "api", "model-context-protocol"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "agents": "^0.16.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.0.0"
  }
}
